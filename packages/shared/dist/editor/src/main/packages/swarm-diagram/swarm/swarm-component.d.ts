import React, { FunctionComponent } from 'react';
import { Swarm } from './swarm';
interface Props {
    element: Swarm;
    children?: React.ReactNode;
    fillColor?: string;
}
export declare const SwarmComponent: FunctionComponent<Props>;
export {};
